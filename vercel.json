{
  "version": 2,
  "buildCommand": "npm install --include=dev && npx vite build --config vite.config.production.ts && npx esbuild server/routes.ts --platform=node --packages=external --bundle --format=esm --outdir=dist && npx esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist",
  "outputDirectory": "dist/public",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}